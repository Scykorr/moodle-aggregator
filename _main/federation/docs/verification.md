# Проверки 08.09.2026

Успешно выполнено:

- 10 тестов `python -m unittest discover -s _main/federation/tests -v`.
- Валидация примерного реестра из трёх подсетей.
- `docker compose config --quiet` с реально сгенерированным окружением.
- Изолированный `tests/smoke_identity.py` на Docker 29.7.2: Keycloak 26.7.3
  и PostgreSQL 17.11 действительно запущены, HTTPS проверен с доверием
  к временному тестовому сертификату без отключения проверки TLS.
- Discovery возвращает ожидаемый issuer; Admin API подтверждает импорт realm,
  точный callback, scopes profile/email и совпадение отдельного client secret
  с сгенерированным значением. Секреты не выводились.
- Проверка состава Git: только небольшие текстовые файлы; ключи, `.env`,
  generated, EXE, образы, архивы и данные исключены.

Тестовый проект использовал случайное имя и свободный localhost-порт; его
контейнеры и volume удаляются в `finally`. Загруженные образы остаются в Docker cache.
Рабочие `moodle_app` и `moodle_db` тест не изменяет.

Отдельно прочитан `public/version.php` локального `moodle_app`:
`5.2.2+ (Build: 20260903)`. Точные релизы удалённых Moodle 3/4, их адреса,
маршрутизация, DNS и CA пока не обследованы.

Не выполнялись: вход реального студента, миграция существующих аккаунтов,
проверка оценок и зачислений, подключение пользовательских Moodle к IdP,
проверка межподсетевого доступа и восстановление промышленной БД.
Эти проверки остаются обязательными перед вводом по [матрице](migration.md).
